// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("bsdiff", "for bsdiff support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("bsdiff", "enable bsdiff support", "no");

if (PHP_BSDIFF != "no") {
	EXTENSION("bsdiff", "bsdiff.c");
}

